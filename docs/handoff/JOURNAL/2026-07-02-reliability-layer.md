# שלב: שדרוג שכבה 1 — אמינות (Reliability) — 2026-07-02

## מטרת השלב
התת-פרויקט הראשון מרודמאפ השדרוג (OR בחר "אמינות"). מטרה: להפוך עדכונים לגלויים ואמינים ולמנוע הישנות שקטה של שתי משפחות-הבאגים שתוקנו ב-2026-07-01 (SW תקוע = באג 2; כפילויות קטלוג = באג 1).

## מה נבנה בפועל (קבצים עיקריים)
- **`vite.config.ts`** — PWA מ-`registerType: 'autoUpdate'` ל-`'prompt'`, והוסר `skipWaiting: true` מ-workbox (כדי שגרסה חדשה "תמתין" והבאנר יופיע; `clientsClaim` נשאר).
- **`src/components/PWAUpdatePrompt.tsx`** — **חדש**. `useRegisterSW` מ-`virtual:pwa-register/react`; באנר "יש גרסה חדשה — רענן" (bottom, RTL, z-120) עם כפתור `updateServiceWorker(true)` + סגירה; בדיקת-עדכון תקופתית (כל שעה) + על focus.
- **`src/App.tsx`** — הרכבת `<PWAUpdatePrompt />` (top-level, always-on, רושם את ה-SW).
- **`src/vite-env.d.ts`** — `/// <reference types="vite-plugin-pwa/react" />`.
- **`tests/priceCatalog/productsCatalog.test.ts`** — **חדש**. מגן-כפילויות: PRODUCTS בלי כפילות supplier|name/id; applyCatalogV1 מנקה+אידמפוטנטי.
- **`tests/priceCatalog/suppliersStore.test.ts`** — +3 טסטים ל-`reconcileCatalogProducts` (התיקון מ-07-01 שלא היה מכוסה): דורס תמיד (גם בגרסה זהה), version מונוטוני, מנקה store מזוהם.

## הדרך: מה נוסה, מה עבד
1. brainstorming → OR בחר את כל 4 מטרות-העל → הצגתי רודמאפ מדורג (4 themes) → OR בחר להתחיל בשכבה 1.
2. מיפוי טכני: אין רישום-SW מותאם (injectRegister auto) → נדרש `registerType: 'prompt'` + hook. `skipWaiting:true` היה מבטל את הבאנר → הוסר.
3. **מגן-כפילויות אומת שהוא תופס:** הזרקתי כפילות זמנית ל-products.ts → 2 טסטים נפלו ("2× ea1") → הוחזר.
4. **מראה הבאנר אומת:** כפייה זמנית של needRefresh ב-dev + בדיקת-DOM (טקסט/כפתורים/RTL/מיקום) → הוחזר.
5. build+typecheck+82 טסטים ירוקים. פריסה (`6a461e88`).

## החלטת-עיצוב מרכזית (+ נימוק)
- **prompt ולא autoUpdate:** הכאב של OR היה *לא לדעת* שיש גרסה חדשה (או להיתקע). באנר-שהמשתמש-לוחץ גלוי ואמין. עלות: הפריסה הראשונה של השינוי היא **עדכון-שקט אחרון** (ה-SW הישן autoUpdate מחליף את עצמו פעם אחרונה); מהפריסה הבאה ואילך הבאנר עובד.

## באגים שהתגלו ותוקנו
- אין. (עבודת-מניעה: מגן-כפילויות + באנר-עדכון מונעים הישנות של באג 1/2.)

## אימות בפרודקשן (deploy 6a461e88)
- ניקוי SW ישן + רענון → הבנדל החדש נטען; **terra=90, אפס כפילויות** (קטלוג תקין), **חיפוש עובד** ("קערה"→5), SW חדש נרשם (1), **אפס שגיאות קונסול**. אין רגרסיה.

## כיוונים שעלו ולא מומשו (+ למה)
- **אופציונליים של שכבה 1** — כפתור אדמין "רענן קטלוג מהשרת" + ניקוי 41 מוצרי-טרה הישנים מ-settings-api. נותרו אופציונליים (reconcile-on-load כבר מכסה); קל להוסיף.
- **שכבות 2-4 של הרודמאפ** (GPT אמיתי, אוטומציות n8n, תובנות/אנליטיקה) — ממתינות בתור; כל אחת = תת-פרויקט נפרד. הרודמאפ המלא: `C:\Users\OR\.claude\plans\proud-twirling-sedgewick.md`.

## לקחים לשלב הבא
- **הפריסה הבאה כלשהי** תהיה זו שבה OR יראה לראשונה את הבאנר "רענן" (הפריסה הנוכחית עברה בשקט בגלל ה-SW הישן). זה תקין.
- מנגנון ה-reconcile + מגן-הכפילויות + הבאנר = משפחות באג 1/2 סגורות מקצה-לקצה.
- הבא ברודמאפ (מומלץ): שכבה 2 — GPT אמיתי בתוך האפליקציה.
