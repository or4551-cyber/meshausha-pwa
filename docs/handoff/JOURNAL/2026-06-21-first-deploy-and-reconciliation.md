# שלב: פריסה ראשונה (Tasks 1–8) + פיוס מול פרודקשן + גילוי חסם ה-billing — 2026-06-21

## מטרת השלב
לפרוס סוף-סוף את כל העבודה (פרוטוקול, מחירון טרה פלסט יוני 2026, ליבת הקטלוג, Task 8) לסניפים. תוך כדי בדיקה לפני הפריסה התגלו **שתי בעיות מהותיות** שמנעו מהעבודה להגיע לאוויר במשך 6 שבועות.

## מה נבנה/קרה בפועל
- **פיוס מול פרודקשן** (`src/data/products.ts`): השוואת המחירים החיים (settings-api) מול קוד-הזרע חשפה 21 מוצרים שקיימים חיים אצל הסניפים אך חסרו מהקוד (נוספו דרך מסך האדמין לאורך זמן). הוחזרו כולם עם ה-id המקורי. הקטלוג גדל 270→291. עודכנו 4 קבצי טסט.
- **פריסה לפרודקשן** דרך Netlify CLI — הצליחה אחרי שדרוג ל-PRO. 21 functions + 81 assets. אומת: catalog/version=1 (נזרע), total=291, settings-api 200.

## הדרך: מה נוסה, מה עבד, מה נכשל ולמה
1. **גילוי #1 — שום דבר לא היה חי:** בדיקה דרך Netlify MCP גילתה שאתר הפרודקשן (`meshaushapp.netlify.app`) תקוע על deploy מ-9 במאי (commit c88e7d1), בלי אף אחת מהפונקציות החדשות. כל ה-`git push` שלנו שמרו ל-GitHub אבל לא נפרסו.
2. **גילוי #2 — הפריסה ידנית:** OR מפרס ידנית (CLI), לא אוטומטית מ-GitHub. המחשב החדש לא הוגדר לכך (אין CLI/טוקן/קישור).
3. **גילוי #3 — פער דאטה:** קריאת המחירים החיים מול הקוד הראתה 21 מוצרים חיים שחסרים מהזרע + 8 מחירי כפפות (העדכון שלנו). פריסה כמו-שזה הייתה מוחקת את ה-21. **תוקן ע"י פיוס.**
4. **גילוי #4 (סיבת השורש האמיתית) — חסם billing:** ניסיון הפריסה נכשל עם `"Account credit usage exceeded - new deploys are blocked until credits are added"`. **זו הסיבה ששום דבר לא נפרס מאז מאי** — החשבון חרג ממכסת ה-dev plan. OR שדרג ל-PRO → הפריסה עברה.

## כיוונים שעלו ולא מומשו (+ למה)
- **סיסמת-מחירים נפרדת ל-Task 9** — OR בחר *לא* (9999 מספיק). משמעות: Task 9 ישתמש בסוד מוטמע (auto-session) במקום מודל סיסמה.
- **auto-deploy מ-GitHub** — לא הוגדר. כרגע פורסים ב-CLI. שווה לשקול חיבור CD בעתיד כדי שלא יהיה צעד ידני.
- **tp4/tp26** — הוחרגו ממחירון יוני אך הוחזרו לבקשת OR ("לשמור הכל").

## באגים/בעיות שהתגלו ותוקנו
- **חסם billing** (סיבת השורש) → שדרוג ל-PRO.
- **פער דאטה של 21 מוצרים** → פיוס לקוד-הזרע.
- **`Product` המקומי ב-products.ts חסר `adminOnly`** → נוסף (מוצר מיקסים adminOnly).
- **VITE_API_TOKEN בזמן build:** הוטמע מפורשות ב-env לפני `npm run build` — אחרת ה-bundle היה עם טוקן ריק ושובר את ה-auth של כל הפונקציות.

## החלטות שהתקבלו (+ נימוק)
- **לפרוס את הכל ביחד** (Tasks 1–8 + Terra + פיוס) — מכיוון ששום דבר לא היה חי, זו הפריסה הראשונה ממילא.
- **לשמור את כל 21 המוצרים החיים** (כולל פלייסהולדרים ₪1 ו-tp4/tp26) — אפס אובדן מידע; OR ינקה דרך מסך האדמין בעתיד.

## פקודת הפריסה (לשימוש חוזר — חשוב!)
פריסה ידנית לפרודקשן מהמחשב הזה:
```
$env:VITE_API_TOKEN = "<מ-Netlify env: VITE_API_TOKEN>"
$env:NETLIFY_AUTH_TOKEN = "<מ-Netlify env: NETLIFY_TOKEN>"
npm run build
npx --yes netlify-cli@latest deploy --prod --no-build --dir dist --functions netlify/functions --site 62cbac42-9bb2-4814-808b-0ef4b78928b5
```
- **חובה** להטמיע VITE_API_TOKEN לפני ה-build (אחרת auth נשבר).
- הסודות זמינים דרך Netlify MCP (`manage-env-vars getAllEnvVars`, siteId 62cbac42-...).
- אימות אחרי deploy: `GET /api/prices/catalog/version` ו-`/api/prices/products?limit=1&includeInactive=true` (total).

## קומיטים רלוונטיים
- `a326e30` [claude] fix: reconcile seed with live production (21 products) before first deploy
- (נפרס לפרודקשן: deploy 6a37c89d1b56a3143a1d28fa)

## לקחים לשלב הבא (Task 9)
- **לפני Task 9:** הקטלוג כבר חי (v1, 291 מוצרים). Task 9 = מסך אדמין שכותב לקטלוג (preview/apply).
- **env vars חסרים ל-Task 9:** `PRICE_SESSION_SECRET`, `PRICE_ADMIN_SECRET`, `PRICE_GPT_TOKEN` — צריך להוסיף ל-Netlify (דרך MCP `manage-env-vars`) לפני שכתיבות עובדות.
- **חוב merge:** ברגע ש-Task 9 יפרסם v2, מכשירים קיימים יאמצו אותו — לכן v2 חייב לכלול את כל המצב הנכון. הקטלוג כבר מפויס (291), אז v2 = 291 + שינוי האדמין. תקין.
- **סיסמה:** OR לא רוצה סיסמת-מחירים נפרדת — auto-session מסוד מוטמע.
- **deploy:** ב-CLI כמתואר למעלה. PRO פעיל עכשיו, אין חסם billing.
